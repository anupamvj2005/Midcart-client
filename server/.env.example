PORT=5000
NODE_ENV=development

# Local:
MONGO_URI=MONGO_URI=mongodb+srv://pharmax:pharmax%40123@cluster11.hpewxcp.mongodb.net/pharmax?retryWrites=true&w=majority
# Atlas (must include database name before ?):
# MONGO_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/smartpharma?retryWrites=true&w=majority

JWT_SECRET=xY7,bqs_iiJB;XGv9sn6edO6IIu6bpBvx{#T2NuY?#0
JWT_EXPIRE=30d

# Optional — leave as placeholders (or omit) to store Rx images on local disk (uploads/prescriptions).
# Set all three from https://cloudinary.com for cloud storage.
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
# If ML runs in Docker and uploads are local, set base URL the ML container can reach, e.g.:
# PUBLIC_BASE_URL=http://host.docker.internal:5000

ML_API_URL=http://localhost:8000

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

CLIENT_URL=http://localhost:5173
